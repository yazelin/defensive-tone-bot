{
  "name": "defensive-tone-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply defensive-tone-bot --local",
    "migrate:remote": "wrangler d1 migrations apply defensive-tone-bot --remote"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^5.7.2",
    "vitest": "2.1.8",
    "wrangler": "^4.107.0"
  }
}